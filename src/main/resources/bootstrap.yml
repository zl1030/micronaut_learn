micronaut:
  application:
    name: micronaut_learn
  config-client:
    enabled: true
consul:
  client:
    registration:
      enabled: true
    config:
      format: yaml
    defaultZone: ${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}
